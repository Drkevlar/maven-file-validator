
@Generated
public final class Flags {

/**
 * Поле isValid является статическим логическим значением,
 * которое указывает на текущую валидность.
 * Значение true означает, что объект является допустимым,
 * а значение false означает, что объект недопустим.
 */
	private static boolean isValid = true;
/**
 * Конструктор класса Flags является приватным и не выполняет никаких действий.
 * Он используется только для того, чтобы запретить создание экземпляров данного класса за его пределами.
 */
	private Flags() { }

/**
 * Метод notValid() устанавливает флаг isValid в значение false.
 * Если флаг уже имеет значение false, то метод ничего не делает.
 */
	public static void notValid() {
		if (isValid) {
			isValid = false;
		}
	}

/**
 * Метод isValid() возвращает текущее значение флага isValid.
 *
 * @return true, если флаг isValid имеет значение true, в противном случае - false.
 */
	public static boolean isValid() {
		return isValid;
	}
}
